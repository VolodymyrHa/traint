pipeline {
    agent { dockerfile true
            docker run -p 80:8080 tomcat }
    stages {
        stage('Test') {
            steps {
                sh 'docker run -p 80:8080 tomcat'
                
            }
        }
    }
}
