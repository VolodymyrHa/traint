pipeline {
    agent {
        docker { image 'nginx' }
    }
    stages {
        stage('Test') {
            steps {
                sh 'COPY nginx.conf /etc/nginx/nginx.conf'
                sh 'docker run --name nginx -d -p 9090:90 nginx'   
            }
        }
    }
}
