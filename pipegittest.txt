pipeline {
    agent { dockerfile true }
    stages {
        stage('Test') {
            steps {
                sh 'docker build -t tomcat'
                sh ' docker run -p 8182:8080 tomcat'
                
            }
        }
    }
}
