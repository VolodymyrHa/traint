node {
    checkout scm
    def testImage = docker.build("volo", "./").withRun('-v /home/sample.war:/usr/local/tomcat/webapps/sample.war -it -p 8585:8080')

    testImage.inside {
        sh 'ls'
    }
}
